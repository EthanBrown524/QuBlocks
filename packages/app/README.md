# @qublocks/app

The Vite app that ties the editor, live simulator, and compiler backends
together into the deployable static site. No backend, no database —
circuits are shared via URL.

Status: not yet implemented.
