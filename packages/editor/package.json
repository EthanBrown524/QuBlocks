{
  "name": "@qublocks/editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@qublocks/ast-schema": "0.0.0",
    "@qublocks/compiler-openqasm": "0.0.0",
    "@qublocks/compiler-qiskit": "0.0.0",
    "blockly": "^13.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^6.0.0",
    "vitest": "^2.0.5"
  }
}
